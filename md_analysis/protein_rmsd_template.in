# Run a simple CA rmsd calc on all trajectories just to confirm...
# no crazy conformational changes occured during the simulations.
# Use the output from trajectory_clean for this.

parm OUT_PATH/Trajectories/Stripped.SYSTEM_apo.prmtop
trajin OUT_PATH/Trajectories/SYSTEM_apo_all_runs.nc

# load pdb of xray structure.
reference OUT_PATH/SYSTEM_apo_postleap.pdb [xrayref]

# Now RMS fit to xray structure.
rms ref [xrayref] @CA
# Now perform actual measurement (so use nofit).
rms nofit rms @CA ref [xrayref] out OUT_PATH/RMSDs/SYSTEM_apo_ca_rmsd.dat
run
clear all
exit
